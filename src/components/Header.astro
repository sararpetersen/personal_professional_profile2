---

---

<header id="header" class="fixed w-full p-4 md:px-16 z-10">
  <div x-data="{ showmenu: false }">
    <div id="navbar" class="flex flex-row justify-between items-center">
      <div id="logo">
        <a href="#top" aria-label="Go to top of page"
          ><img
            src="/illustrations/logo-srp-white.svg"
            alt="Logo for my website, with three of my initials (white)"
            aria-hidden="true"
            class="h-16 transition duration-300 ease-out group-hover:-translate-y-[2px] group-hover:scale-[1.02]"
            width="64"
            height="64"
            loading="eager"
          /></a
        >
      </div>
      <nav id="desktopmenu" aria-label="Main navigation" class="hidden lg:block text-sm font-medium text-pale-50">
        <ul class="flex items-center gap-8">
          <li><a href="#info" class="nav-link uppercase">Who am I?</a></li>
          <li><a href="#timeline" class="nav-link uppercase">Timeline</a></li>
          <li><a href="#skills" class="nav-link uppercase">Skills</a></li>
          <li><a href="#projects" class="nav-link uppercase">Selected Cases</a></li>
          <li><a href="#contact" class="nav-link uppercase">Contact</a></li>
        </ul>
      </nav>
      <button
        id="burgermenu"
        aria-label="Toggle mobile menu"
        aria-controls="mobilmenu"
        x-bind:aria-expanded="showmenu ? 'true' : 'false'"
        class="lg:hidden"
        x-on:click="showmenu = !showmenu"
      >
        <svg
          x-show="!showmenu"
          x-cloak
          xmlns="http://www.w3.org/2000/svg"
          fill="none"
          viewBox="0 0 24 24"
          stroke-width="1.5"
          stroke="currentColor"
          class="size-8 hover:text-blue-200 transition duration-300"
        >
          <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5"></path>
        </svg>
        <svg
          x-show="showmenu"
          x-cloak
          xmlns="http://www.w3.org/2000/svg"
          fill="none"
          viewBox="0 0 24 24"
          stroke-width="1.5"
          stroke="currentColor"
          class="size-8 hover:text-blue-200 transition duration-300"
        >
          <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"></path>
        </svg>
      </button>
    </div>
    <nav
      id="mobilmenu"
      aria-label="Mobile navigation"
      class="flex flex-col content-center pt-4"
      x-show="showmenu"
      x-cloak
      x-transition
      x-on:click="showmenu = false"
      x-on:click.outside="showmenu = false"
      x-on:keydown.escape.window="showmenu = false"
    >
      <ul>
        <li class="text-center py-3 border-b border-blue-200/20 transition duration-300">
          <a href="#info" class="hover:text-blue-200 transition duration-300">WHO AM I?</a>
        </li>
        <li class="text-center py-3 border-b border-blue-200/20 transition duration-300">
          <a href="#timeline" class="hover:text-blue-200 transition duration-300">TIMELINE</a>
        </li>
        <li class="text-center py-3 border-b border-blue-200/20 transition duration-300">
          <a href="#skills" class="hover:text-blue-200 transition duration-300">SKILLS</a>
        </li>
        <li class="text-center py-3 border-b border-blue-200/20 transition duration-300">
          <a href="#projects" class="hover:text-blue-200 transition duration-300">MY PROJECTS</a>
        </li>
        <li class="text-center py-3 border-b border-blue-200/20 transition duration-300">
          <a href="#contact" class="hover:text-blue-200 transition duration-300">CONTACT</a>
        </li>
      </ul>
    </nav>
  </div>
</header>

<style>
  [x-cloak] {
    display: none !important;
  }

  .nav-link {
    position: relative;
    padding: 0.5rem 0;
  }

  .nav-link::after {
    content: "";
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 2px;
    background-color: #c0d7e1;
    transform: scaleX(0);
    transform-origin: right;
    transition: transform 0.3s ease-in-out;
  }

  .nav-link:hover,
  .nav-link:focus-visible {
    color: #c0d7e1;
  }

  .nav-link:hover::after,
  .nav-link:focus-visible::after {
    transform-origin: left;
    transform: scaleX(1);
  }

  /* Border-only header treatment for the dark background */
  #header {
    background-color: rgba(16, 24, 32, 0.86);
    backdrop-filter: blur(12px);
    border-bottom: 1px solid rgba(192, 215, 225, 0.12);
    transition:
      background-color 0.3s ease,
      border-color 0.3s ease;
  }

  #header.header-scrolled {
    background-color: rgba(16, 24, 32, 0.96);
    border-bottom-color: rgba(192, 215, 225, 0.24);
  }
</style>

<script>
  const header = document.getElementById("header");
  const updateHeaderStyle = () => {
    if (window.scrollY > 10) {
      header?.classList.add("header-scrolled");
    } else {
      header?.classList.remove("header-scrolled");
    }
  };
  window.addEventListener("scroll", updateHeaderStyle, { passive: true });
  updateHeaderStyle();
</script>
